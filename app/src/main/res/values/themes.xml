<resources xmlns:tools="http://schemas.android.com/tools">
    
    <!-- Main Application Theme -->
    <style name="Theme.Codemy" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/rubik</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
        <item name="android:background">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
        
        <!-- Button styles -->
        <item name="materialButtonStyle">@style/CodemyButton.Primary</item>
        
        <!-- Color mappings -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryVariant">@color/colorAccentDark</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnPrimary">@color/colorBackground</item>
        <item name="colorOnSecondary">@color/colorTextPrimary</item>
    </style>

    <!-- No ActionBar Theme -->
    <style name="Theme.Codemy.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.Codemy.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorAccent</item>
    </style>

</resources>
